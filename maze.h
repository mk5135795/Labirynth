#ifndef MAZE_H
#define MAZE_H

#include "macro.h"
#include "map.h"

void genmaze(map_t *mp); //53
void rwalk3(map_t *mp); //112
void randmap(map_t *map);
void checkers(map_t *map);
void grid(map_t *map);

#endif /*MAZE_H*/
